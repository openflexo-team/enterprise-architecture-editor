dependencies {
  // openflexo dep
  implementation flexoFoundation()
  implementation flexoUi()
  implementation diagramConnector()
  implementation diagramConnectorUi()
  implementation ginaConnector()
  implementation ginaConnectorUi()
  // The BPMN modeler: a self-contained project carrying its API, its resource center (which must
  // always be present at run-time) and the EMF / GINA connectors its VirtualModels mount. It used
  // to be reached through modelers-api + modelers-rc, where it shared the
  // http://openflexo.org/modellers resource center with the StateCharts, UML and OWL modelers.
  implementation openflexo("bpmn-modeler")

  // external dep
  // testing dep
  testImplementation flexoUiTest()
  testImplementation flexoFoundationTest()
}
